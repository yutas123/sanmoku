import { useState } from "react";

const person = {
  name: "John Doe",
  age: 30,
  country: "USA"
}

const objectKeys = Object.keys(person);
console.log(Object);
// console.log(objectKeys); // ["name", "age", "country"]

export default ContactForm;